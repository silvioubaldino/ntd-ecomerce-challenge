# Local development environment for the api service.
# Copy to .env and adjust as needed. docker-compose sets these itself.
API_PORT=8080
DATABASE_URL=postgres://ntd:ntd@localhost:5432/ntd_ecomerce?sslmode=disable
